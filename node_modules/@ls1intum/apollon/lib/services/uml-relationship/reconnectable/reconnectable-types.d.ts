import { Action } from '../../../utils/actions/actions';
import { Connection } from '../connection';
import { IUMLRelationship } from '../uml-relationship';
export declare const enum ReconnectableActionTypes {
    START = "@@element/reconnectable/START",
    END = "@@element/reconnectable/END",
    RECONNECT = "@@element/reconnectable/RECONNECT"
}
export declare type ReconnectableState = {
    [id: string]: 'source' | 'target';
};
export declare type ReconnectableActions = ReconnectStartAction | ReconnectEndAction | ReconnectAction;
export declare type ReconnectStartAction = Action<ReconnectableActionTypes.START> & {
    payload: {
        ids: string[];
        endpoint: 'source' | 'target';
    };
};
export declare type ReconnectEndAction = Action<ReconnectableActionTypes.END> & {
    payload: {
        ids: string[];
    };
};
export declare type ReconnectAction = Action<ReconnectableActionTypes.RECONNECT> & {
    payload: {
        connections: (Pick<IUMLRelationship, 'id'> & Connection)[];
    };
};
