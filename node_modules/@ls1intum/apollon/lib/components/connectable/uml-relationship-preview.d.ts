import { Component } from 'react';
import { Direction, IUMLElementPort } from '../../services/uml-element/uml-element-port';
import { UMLElementRepository } from '../../services/uml-element/uml-element-repository';
import { AsyncDispatch } from '../../utils/actions/actions';
import { Point } from '../../utils/geometry/point';
declare type OwnProps = {
    port: IUMLElementPort;
    target: Point;
};
declare type StateProps = {
    ports: {
        [key in Direction]: Point;
    };
};
declare type DispatchProps = {
    end: AsyncDispatch<typeof UMLElementRepository.endConnecting>;
    getAbsolutePosition: AsyncDispatch<typeof UMLElementRepository.getAbsolutePosition>;
};
declare type Props = OwnProps & StateProps & DispatchProps;
declare class RelationshipPreview extends Component<Props> {
    render(): JSX.Element;
}
export declare const UMLRelationshipPreview: import("react-redux").ConnectedComponentClass<typeof RelationshipPreview, Pick<Props, "target" | "port"> & OwnProps>;
export {};
