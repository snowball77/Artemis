import { Component, RefObject } from 'react';
import { ILayer } from '../../services/layouter/layer';
import { IUMLDiagram } from '../../services/uml-diagram/uml-diagram';
import { Point } from '../../utils/geometry/point';
declare type OwnProps = {};
declare type StateProps = {
    diagram: IUMLDiagram;
    isStatic: boolean;
};
declare type DispatchProps = {};
declare type Props = OwnProps & StateProps & DispatchProps;
export declare class CanvasComponent extends Component<Props> implements Omit<ILayer, 'layer'> {
    layer: RefObject<SVGSVGElement>;
    origin: () => Point;
    snap: (point: Point) => Point;
    render(): JSX.Element;
}
export declare const Canvas: import("react-redux").ConnectedComponentClass<typeof CanvasComponent, Pick<StateProps, never>>;
export {};
