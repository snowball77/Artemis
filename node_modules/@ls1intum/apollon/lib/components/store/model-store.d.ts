import React, { Component, PropsWithChildren } from 'react';
import { DeepPartial, Store } from 'redux';
import { Actions } from '../../services/actions';
import { ILayer } from '../../services/layouter/layer';
import { CanvasContext } from '../canvas/canvas-context';
import { ModelState } from './model-state';
declare type OwnProps = PropsWithChildren<{
    initialState?: DeepPartial<ModelState>;
}>;
declare type Props = OwnProps & CanvasContext;
declare const getInitialState: (initialState?: DeepPartial<ModelState>, layer?: ILayer | null) => {
    store: Store<ModelState, Actions>;
};
declare type State = ReturnType<typeof getInitialState>;
export declare class ModelStore extends Component<Props, State> {
    state: {
        store: Store<ModelState, Actions>;
    };
    componentDidUpdate(prevProps: Props): void;
    render(): JSX.Element;
}
export declare const StoreProvider: React.ForwardRefExoticComponent<Pick<Props, "children" | "initialState"> & React.RefAttributes<React.Component<{}, {}, any>>>;
export {};
